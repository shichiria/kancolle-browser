# レビュー指示書 (failures.jsonl から自動生成)
Generated: 2026-04-04 | 分析した失敗: 8件

## 絶対ルール (severity=critical/high)

- ゲームのJS環境にコードを注入する手法は絶対に提案しない。外部（capture/proxy/OS hook）からのみ情報を取得する
  - 根拠: ゲームJS環境へのコード注入を提案し、何度も方針を修正された (2026-03-30)
- APIデータの流れを正確に把握してから実装する。フィールドがどの時点で設定されるか確認すること
  - 根拠: 戦闘情報オーバーレイが表示されない。ユーザーが3回「出ません」と報告 (2026-03-23)
- .gitattributesの設定変更は既存ファイルに自動適用されない。作業ディレクトリのファイルも実際に変換が必要。git config設定の副作用を正確に把握すること
  - 根拠: CRLF→LF変換で3回修正指示。gitattributes追加だけで既存ファイルが変換されたと報告 (2026-03-19)
- ユーザーの最初の指示が特定のケースでも、汎用化の意図がないか確認する。実装手順はユーザーの進め方に従い、先走って設計を固めない
  - 根拠: UIイベント検出の設計でユーザーの意図を何度も誤解。スコープ・手順・目的を3回以上修正 (2026-03-30)

## 注意事項 (severity=medium/low)

- Dev起動前にkancolle-browser.exe, cargo.exe, node.exeを全てkillし、ポート1420の解放を確認してから起動する
- テストfixtureやサンプルデータは最新のものを優先して取得する。ソートして最新を選ぶこと
- トグル機能を実装する際は、再有効化時のデータ再表示も考慮する。既存の類似機能（ミニマップ）と同じパターンを適用すること
- スキル作成時は入力元・出力先・後処理（退避等）の3点をユーザーに確認してから作成する

## カテゴリ別パターン

### bug (3件)
- Dev起動前にkancolle-browser.exe, cargo.exe, node.exeを全てkillし、ポート1420の解放を確認してから起動する
- APIデータの流れを正確に把握してから実装する。フィールドがどの時点で設定されるか確認すること
- トグル機能を実装する際は、再有効化時のデータ再表示も考慮する。既存の類似機能と同じパターンを適用すること

### prompt_ignored (3件)
- ゲームのJS環境にコードを注入する手法は絶対に提案しない。外部からのみ情報を取得する
- ユーザーの指示が特定ケースでも汎用化の意図がないか確認する。先走って設計を固めない
- スキル作成時は入力元・出力先・後処理の3点をユーザーに確認してから作成する

### config_error (1件)
- git設定変更は既存ファイルに自動適用されない。実際の変換も必要。設定の副作用を正確に把握すること

### design_flaw (1件)
- テストfixtureやサンプルデータは最新のものを優先して取得する。ソートして最新を選ぶこと

## このプロジェクト固有の注意点

### WebView2 / Tauri
- (該当する失敗なし — ただしCAUTION.mdの「Win WebView2+proxy = DEADLOCK」は常に注意)

### 艦これドメイン
- ゲームJS環境へのコード注入は絶対禁止（BOT規約違反回避のため）
- APIフィールドのライフサイクルを把握してから実装する（例: node.battleはon_battle_result()で初めて設定される）
- 制空状態などAPIが返す値はフィルタせずそのまま表示してよい（双方制空値0でも「航空優勢」が返る仕様）
