{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "game-controls",
  "description": "Capability for the game window (zoom/mute controls)",
  "windows": ["game"],
  "permissions": [
    "core:default"
  ]
}
