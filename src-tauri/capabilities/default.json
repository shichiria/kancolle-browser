{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the React-app windows (management + kantai)",
  "windows": ["management", "kantai"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default"
  ]
}
