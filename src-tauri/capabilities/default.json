{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the React-app windows (management + kantai + quests + improvement + ships)",
  "windows": ["management", "kantai", "quests", "improvement", "ships"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default"
  ]
}
